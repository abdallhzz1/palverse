# Archives / accidental dumps (block GitHub push)
*.zip
*.7z
*.rar

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store

# Local env
.env
.env.local
.env.*.local

# Accidental root npm lockfile (apps own their lockfiles; no root package.json)
/package-lock.json
