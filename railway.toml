[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["palverse-api/**", "Dockerfile", "railway.toml"]

[deploy]
# Rely on Dockerfile CMD (absolute path). No healthcheck until DB vars are confirmed.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
